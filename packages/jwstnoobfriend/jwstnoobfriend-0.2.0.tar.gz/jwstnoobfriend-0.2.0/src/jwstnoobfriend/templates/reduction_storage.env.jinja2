{% if replace %}
# Path to the File Box tool
FILE_BOX_PATH={{ file_box_path | default('') }}
# Path to the auxiliary files directory
AUXILIARY_PATH={{ auxiliary_path | default('') }}

# Paths to save each level of the reduced data
{% endif %}
{% for stage, path in stage_folder_paths.items() %}
STAGE_{{ stage | upper }}_PATH={{ path }}
{% endfor %}
