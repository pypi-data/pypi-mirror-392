apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hyperopt-claim
  namespace: _placeholder_  # set by the optimiser at runtime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
