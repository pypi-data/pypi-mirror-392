[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "aiocache_pro"
version = "0.0.1"
description = "High-performance caching library with gevent and cython speedups"
authors = [{ name = "Maksym" }]
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = ["gevent", "ujson", "b64fx"]