[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "alphaaidig-datacenter-common"
version = "0.1.23"
description = "Shared schemas and utils for the datacenter project."
authors = [
    {name = "Hang GuangLiang", email = "hanguangliang@alphaaidig.com"},
]
dependencies = [
    "pydantic>=2.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
index-url = "https://pypi.tuna.tsinghua.edu.cn/simple"