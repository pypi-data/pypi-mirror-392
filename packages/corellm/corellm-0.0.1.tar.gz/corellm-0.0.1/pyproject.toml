[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "corellm"
version = "0.0.1"
description = "An AI library (in development)"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{ name="Jadon Duff" }]
keywords = ["llm", "placeholder"]

[project.urls]
"Homepage" = "https://github.com/jadonduff/pyllm"
