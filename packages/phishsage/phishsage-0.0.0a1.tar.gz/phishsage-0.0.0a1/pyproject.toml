[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "phishsage"
version = "0.0.0a1"
description = "Placeholder package to reserve the name phishsage"
authors = [{name = "Adams"}]
readme = "README.md"
