[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "artimers"
version = "1.0.0"
description = "A utility package for timing and managing timers."
authors = [
    { name="Alexander Rießbeck", email="alexanderriessbeck@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []
keywords=["Timer", "Timing", "class", "contextmanager", "decorator"]

[project.urls]
Homepage="https://gitlab.com/alexander-riessbeck/artimer"
Source="https://gitlab.com/alexander-riessbeck/artimer"
Issues="https://gitlab.com/alexander-riessbeck/artimer/issues"