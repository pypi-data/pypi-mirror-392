[project]
name = "fastinit-cli"
version = "0.1.0"
description = "A simple CLI to quickly and cleanly create FastAPI projects and modules."
authors = [
    { name="João Pedro de Oliveira Matos", email="joaopedrooliveira030506@gmail.com" }
]
readme = "readme.md"
requires-python = ">=3.8"

[project.scripts]
fastinit = "fastinit.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
