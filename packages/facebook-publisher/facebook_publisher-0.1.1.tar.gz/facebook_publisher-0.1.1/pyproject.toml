[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "facebook-publisher"                   # Nombre EXACTO en PyPI
version = "0.1.1"
description = "SDK para publicar en grupos de Facebook con Playwright"
readme = "README.md"
authors = [
  {name="espinosalastresr", email="espinosalastresr@gmail.com"},
]
license = {file = "LICENSE"}
requires-python = ">=3.10"

dependencies = [
  "playwright>=1.40",
]
