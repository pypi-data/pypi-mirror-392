[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "onflow-wms-shared-auth"
version = "0.1.6"
description = "Shared authentication module for Onflow WMS"
authors = [
    { name = "N-H Logistics", email = "dev@nhlogistics.vn" }
]
requires-python = ">=3.9"

dependencies = [
]
