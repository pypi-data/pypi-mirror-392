# Zephyr AI Ray Placeholder

This distribution simply reserves the `zephyr-ai-ray` name on PyPI. A future release
will ship the real Ray executor integration once it stabilizes. Until then, importing
`zephyr_ray` will raise a helpful error message.
