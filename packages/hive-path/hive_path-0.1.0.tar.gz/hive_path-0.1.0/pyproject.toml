[project]
name = "hive-path"
version = "0.1.0"
description = "A simple package pathlib extension for paths with Hive-style partitioning."
readme = "README.md"
authors = [
    { name = "Ben", email = "ishmandoo@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
