[project]
name = "fastapi-opinionated-core"
version = "0.1.2"
description = ""
authors = [
    {name = "Azzarnuji",email = "azzarnuzy93@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all] (>=0.121.1,<0.122.0)"
]

[tool.poetry]
packages = [
    { include = "fastapi_opinionated" }
]

[tool.poetry.group.dev.dependencies]
twine = "^6.2.0"
typer = "^0.20.0"

[tool.poetry.scripts]
fastapi-opinionated = "fastapi_opinionated.cli.main:main"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
