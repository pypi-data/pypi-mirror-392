# Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True

# Axioms Configuration
# Use jwtforge.dev for testing
AXIOMS_AUDIENCE=https://api.example.com

# IMPORTANT: Use explicit ISS and JWKS URLs for jwtforge.dev
# Issuer URL must match the token's iss claim
AXIOMS_ISS_URL=https://jwtforge.dev

# JWKS URL for fetching public keys
AXIOMS_JWKS_URL=https://jwtforge.dev/.well-known/jwks.json

# Optional: AXIOMS_DOMAIN (not needed when using explicit URLs)
# AXIOMS_DOMAIN=jwtforge.dev

# Optional: Custom claim names (uncomment if using custom claims)
# AXIOMS_SCOPE_CLAIMS=scope,scp
# AXIOMS_ROLES_CLAIMS=roles,https://example.com/roles
# AXIOMS_PERMISSIONS_CLAIMS=permissions,https://example.com/permissions
