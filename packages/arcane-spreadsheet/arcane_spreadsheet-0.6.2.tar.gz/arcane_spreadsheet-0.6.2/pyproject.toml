[tool.poetry]
name = "arcane-spreadsheet"
version = "0.6.2"
description = "Arcane spreadsheet tools"
readme = "README.md"
authors = ["Arcane <product@wearcane.com>"]
packages = [
    { include = "arcane" }
]

[tool.poetry.dependencies]
python = "^3.6"

google-api-python-client = "^2.40.0"
oauth2client = "4.1.3"
arcane-core = "^1.6.0"
arcane-requests = ">=0.0.0,<1.0.0"
[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
