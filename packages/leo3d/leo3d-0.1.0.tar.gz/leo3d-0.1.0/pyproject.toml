[project]
name = "leo3d"
version = "0.1.0"
description = "Python + Native OpenGL 3D Engine"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "pybind11"]
build-backend = "setuptools.build_meta"