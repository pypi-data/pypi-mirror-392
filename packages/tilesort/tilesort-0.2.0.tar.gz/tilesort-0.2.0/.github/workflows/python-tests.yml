name: Python Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.8', '3.9', '3.10', '3.11', '3.12', '3.13', '3.14']

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Set up Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
      with:
        toolchain: stable

    - name: Install uv
      uses: astral-sh/setup-uv@v4

    - name: Install dependencies and create venv
      run: uv sync --group dev

    - name: Install maturin in venv
      run: uv pip install maturin

    - name: Build Python package
      run: uv run maturin develop --features python

    - name: Run Python tests
      run: uv run --group dev pytest python/tests/ -v

    - name: Run type checking
      run: uv run --group dev mypy python/
      # Only run type checking on latest Python to save CI time
      if: matrix.python-version == '3.14' && matrix.os == 'ubuntu-latest'
