version: 2

sources:
  - name: raw_data
    description: "Raw extracted tables prior to transformation."
    tables:
      - name: employees_tbl
        description: "Raw employees export"
      - name: works_for_tbl
        description: "Raw join table mapping employees to companies"
