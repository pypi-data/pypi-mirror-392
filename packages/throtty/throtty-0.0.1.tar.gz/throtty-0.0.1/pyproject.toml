[tool.poetry]
name = "throtty"
version = "0.0.1"
description = "Rate limit library for ASGI application"
authors = ["DickyAdi <dickyadi44@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "core"}]

[tool.poetry.dependencies]
python = "^3.12"
sortedcontainers = "^2.4.0"
redis = "^7.0.1"

[tool.poetry.group.dev.dependencies]
pytest = "^9.0.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"