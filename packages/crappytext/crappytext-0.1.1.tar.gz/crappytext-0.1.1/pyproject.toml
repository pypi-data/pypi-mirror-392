[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crappytext"
version = "0.1.1"
authors = [{name = "jacklak"}]
description = "The world's crappiest text editor"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
dependencies = [
    "prompt_toolkit>=3.0.0"
]
[project.scripts]
crappytext = "crappytext.core:main"