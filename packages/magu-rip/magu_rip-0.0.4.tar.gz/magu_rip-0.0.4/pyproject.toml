[project]
name = "magu-rip"
version = "0.0.4"
description = "Simple Web Framework."
dependencies = [
    "cffi",
    "cryptography",
    "pycparser",
    "PyJWT",
    "build",
    "colorama",
    "mysql-connector-python",
    "packaging",
    "pyproject_hooks",
]

[tool.setuptools.packages.find]
where = ["src"]
