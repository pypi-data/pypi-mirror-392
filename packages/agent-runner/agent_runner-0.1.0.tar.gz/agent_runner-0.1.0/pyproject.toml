[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-runner"
version = "0.1.0"
description = "open sourcing soon."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Kamryn Ohly", email = "kamryn@arcada.dev"}
]

[tool.setuptools.packages.find]
where = ["src"]
