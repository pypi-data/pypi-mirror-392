[project]
name = "fish_log"
version = "0.1.0"
description = "A simple logging package"
authors = [{ name = "Bas van wees" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
fish-log = "fish_log.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
