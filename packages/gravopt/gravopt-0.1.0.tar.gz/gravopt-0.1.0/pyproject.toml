[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gravopt"
version = "0.1.0"
description = "A short description of what your package does."
authors = [
    {name = "Dimitar Kretske", email = "kretski1@gmail.com"},
]
readme = "README.md"
license = {text = "MIT"}  # или друга лицензия
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.8"  # или нужната ти версия
# Ако имаш зависимости, ги добави тук:
# dependencies = [
#     "numpy<2.0",
#     "torch",
#     # ...
# ]

# [project.urls]
# Homepage = "https://github.com/Kretski/GravOptAdaptiveE"
# Repository = "https://github.com/Kretski/GravOptAdaptiveE"
