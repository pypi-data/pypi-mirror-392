# coding: utf-8

"""
    RankVectors API

    Intelligent internal linking optimization API using AI.   RankVectors helps you automatically discover and implement optimal internal links  across your website to improve SEO performance and user experience.  ## Key Features - **AI-Powered Analysis**: Uses OpenAI embeddings to find optimal linking opportunities - **Smart Crawling**: Automatically crawls and analyzes your website content - **Automated Implementation**: Implement links via webhooks or manual instructions - **Page-Based Plans**: Predictable pricing by number of pages monitored - **Multi-Platform Support**: Works with any CMS or platform via REST API  ## Getting Started 1. Create a project with your website URL 2. Start a crawl to analyze your content 3. Generate AI-powered link suggestions 4. Implement suggestions via API or webhook 5. Track performance and manage page usage and limits  ## Authentication Most API endpoints support authentication using your RankVectors API key. Include your API key in the `Authorization` header: ``` Authorization: Bearer YOUR_API_KEY ```  Get your API key from your RankVectors dashboard: Settings → API Keys  **Note**: Some endpoints (marked in the documentation) support both API key authentication and web session authentication (Stack Auth).  API key authentication is required for SDK usage and external integrations like WordPress plugins. 

    The version of the OpenAPI document: 1.3.1
    Contact: tj@rankvectors.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from rankvectors.models.rollback import Rollback
from rankvectors.models.suggestion import Suggestion
from typing import Optional, Set
from typing_extensions import Self

class Implementation(BaseModel):
    """
    Implementation
    """ # noqa: E501
    id: StrictStr = Field(description="Unique implementation identifier")
    status: StrictStr = Field(description="Implementation status")
    platform: StrictStr = Field(description="Platform used")
    implementation_method: Optional[StrictStr] = Field(default=None, description="Implementation method", alias="implementationMethod")
    credits_used: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Credits consumed", alias="creditsUsed")
    created_at: datetime = Field(description="Implementation start timestamp", alias="createdAt")
    completed_at: Optional[datetime] = Field(default=None, description="Implementation completion timestamp", alias="completedAt")
    metadata: Optional[Dict[str, Any]] = Field(default=None, description="Platform-specific metadata")
    suggestion: Suggestion
    rollbacks: Optional[List[Rollback]] = Field(default=None, description="Rollback history")
    __properties: ClassVar[List[str]] = ["id", "status", "platform", "implementationMethod", "creditsUsed", "createdAt", "completedAt", "metadata", "suggestion", "rollbacks"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pending', 'in_progress', 'success', 'failed', 'rolled_back']):
            raise ValueError("must be one of enum values ('pending', 'in_progress', 'success', 'failed', 'rolled_back')")
        return value

    @field_validator('platform')
    def platform_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['custom']):
            raise ValueError("must be one of enum values ('custom')")
        return value

    @field_validator('implementation_method')
    def implementation_method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['api', 'manual']):
            raise ValueError("must be one of enum values ('api', 'manual')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Implementation from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of suggestion
        if self.suggestion:
            _dict['suggestion'] = self.suggestion.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in rollbacks (list)
        _items = []
        if self.rollbacks:
            for _item_rollbacks in self.rollbacks:
                if _item_rollbacks:
                    _items.append(_item_rollbacks.to_dict())
            _dict['rollbacks'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Implementation from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "status": obj.get("status"),
            "platform": obj.get("platform"),
            "implementationMethod": obj.get("implementationMethod"),
            "creditsUsed": obj.get("creditsUsed"),
            "createdAt": obj.get("createdAt"),
            "completedAt": obj.get("completedAt"),
            "metadata": obj.get("metadata"),
            "suggestion": Suggestion.from_dict(obj["suggestion"]) if obj.get("suggestion") is not None else None,
            "rollbacks": [Rollback.from_dict(_item) for _item in obj["rollbacks"]] if obj.get("rollbacks") is not None else None
        })
        return _obj


