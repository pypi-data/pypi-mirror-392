[project]
name = "fastapi-opinionated-socket"
version = "0.1.0"
description = ""
authors = [
    {name = "Azzarnuji",email = "azzarnuzy93@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-socketio (>=5.14.3,<6.0.0)",
    "python-engineio (>=4.12.3,<5.0.0)",
    "fastapi-opinionated-core (>=0.1.2,<0.2.0)",
]
packages = [
    { include = "fastapi_opinionated_socket" }
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]

[tool.poetry.group.dev.dependencies]
twine = "^6.2.0"

