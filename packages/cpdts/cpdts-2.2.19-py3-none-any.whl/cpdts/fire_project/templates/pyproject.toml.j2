[project]
name = "{{project_name}}"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lipanpan", email = "lipanpanmail@163.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fire>=0.7.1",
    "toml>=0.10.2",
]

[project.scripts]
{{ project_name }} = "{{ project_name }}:main"
# clean = "scripts.clean:clean"
# build = "scripts.build:build"
# public = "scripts.public:public"
ccx = "scripts.ccx:ccx"

[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"

[tool.ruff]
ignore = [
    "F541", # fstring字符串当中没有使用变量
    "E722", # 裸的 excpet 
]