[project]
name = "test_sdk_kv"
version = "0.0.1"
description = "A very basic Python SDK"
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["requests"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
