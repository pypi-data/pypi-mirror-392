[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "casetools"
version = "1.0.1"
description = "A lightweight library for controlling text."
readme = "README.md"
authors = [{ name="Johnathan31"}]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[project.urls]
"Homepage" = "https://github.com/johnathan31/casetools"

[tool.pytest.ini_options]
testpaths = ["tests"]

