[project]
name = "iflow-mcp_reader-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "mcp[cli]>=1.4.1",
]

[project.scripts]
reader-mcp-server = "main:main"

[tool.setuptools]
py-modules = ["main", "models"]
