[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "qmatsuite"
version = "0.0.1"
description = "Placeholder package for QMatSuite — Quantum Materials Simulation & Integrative Environment."
authors = [
  { name = "QMatSuite Developers" }
]
readme = "README.md"
requires-python = ">=3.8"
