import { auth } from './auth'
import { scheduler } from './scheduler'
import { ecosystem, project, repository } from './datasources'
import { client } from './client'

export const API = {
  auth,
  scheduler,
  ecosystem,
  project,
  repository,
  client
}
