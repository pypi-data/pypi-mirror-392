[project]
name = "techfactsdb"
version = "0.1.0"
description = "My custom module"
authors = [{name = "Abhiram"}]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
