[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pymcdc"
version = "0.2"
description = "MC/DC analizer for Python"
authors = [
    {name = "Delamaro", email = "delamaro@icmc.usp.br"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
scripts = { pymcdc = "pymcdc.__main__:main" }

[tool.setuptools.packages.find]
where = ["."]
