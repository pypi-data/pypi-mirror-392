[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "create-st"
version = "0.1.5"
description = "CLI tool for generating directory structures from a text file"
readme = "README.md"
authors = [{name = "aloha v2"}]
requires-python = ">=3.7"

[project.scripts]
create-st = "create_st.cli:main"