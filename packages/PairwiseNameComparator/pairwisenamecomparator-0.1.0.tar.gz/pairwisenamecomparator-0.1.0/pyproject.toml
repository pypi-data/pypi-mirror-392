[build-system]
requires = ["setuptools", "wheel", "Cython>=3.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "PairwiseNameComparator"
version = "0.1.0"
description = "Fast parallel dictionary lookups using Cython and OpenMP"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []