# Bookmark-Lens Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# Database Paths
# ============================================================================

# Path to DuckDB database file (stores bookmark metadata, tags, etc.)
BOOKMARK_LENS_DB_PATH=./data/bookmark_lens.db

# Path to LanceDB directory (stores vector embeddings)
LANCE_DB_PATH=./data/embeddings.lance


# ============================================================================
# Embedding Configuration
# ============================================================================

# Sentence-transformers model name
# Options:
#   - all-MiniLM-L6-v2 (384D) - Fast, good quality, recommended for most use cases
#   - all-mpnet-base-v2 (768D) - Slower but higher quality
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2

# Vector dimension (MUST match the model above)
#   - all-MiniLM-L6-v2: 384
#   - all-mpnet-base-v2: 768
EMBEDDING_DIMENSION=384


# ============================================================================
# Content Fetching
# ============================================================================

# HTTP fetch timeout in seconds
BOOKMARK_LENS_FETCH_TIMEOUT=30

# User-Agent string for HTTP requests
BOOKMARK_LENS_USER_AGENT=bookmark-lens/0.1.0

# Maximum characters to store per bookmark (prevents memory issues with huge pages)
MAX_CONTENT_LENGTH=50000


# ============================================================================
# LLM Integration (Smart Mode - Phase 2)
# ============================================================================

# Enable Smart Mode by setting these variables
# Smart Mode adds: auto-summaries, auto-tags, topic classification

# LLM Model to use (required for Smart Mode)
# Examples:
#   - claude-3-haiku-20240307 (Anthropic - fast, cheap, recommended)
#   - gpt-4o-mini (OpenAI - fast, cheap)
#   - gpt-4o (OpenAI - better quality, more expensive)
#   - claude-3-5-sonnet-20241022 (Anthropic - best quality)
# See https://docs.litellm.ai/docs/providers for full list
LLM_MODEL=claude-3-haiku-20240307

# API Key for your LLM provider (required for Smart Mode)
# Get keys from:
#   - Anthropic: https://console.anthropic.com/
#   - OpenAI: https://platform.openai.com/api-keys
LLM_API_KEY=

# Optional: Custom API endpoint (for self-hosted models)
# LLM_API_BASE=

# Optional: Temperature (0.0-1.0, default: 0.3)
# Lower = more consistent, Higher = more creative
# LLM_TEMPERATURE=0.3


# ============================================================================
# Notes
# ============================================================================

# - All paths are relative to the project root unless absolute
# - data/ directory is gitignored by default
# - Smart Mode (LLM features) is optional; core features work without any API keys
# - First run will download the embedding model (~80MB for MiniLM-L6-v2)
