[project]
name = "fastcrop"
version = "0.1.0"
description = "A minimal GUI app for image cropping"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
  "pytest",
  "ruff",
  "ty",
]

[project.scripts]
fastcrop = "fastcrop.gui:main"

[tool.uv]
package = true
