[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ultrazoom"
description = "A fast single image super-resolution (SISR) model for upscaling images with ultra high-quality."
version = "0.2.1"
requires-python = ">= 3.10"
dependencies = [
  "torch~=2.9.0",
  "huggingface_hub~=0.34.0",
  "safetensors~=0.4.3"
]
authors = [
  {name = "Andrew DalPino"},
]
readme = "README.md"
license = {text = "MIT"}

[project.optional-dependencies]
dev = ["mypy", "black", "build", "twine"]
test = ["mypy", "black"]

[project.urls]
Homepage = "https://github.com/andrewdalpino/UltraZoom"
Documentation = "https://github.com/andrewdalpino/UltraZoom/README.md"
Source = "https://github.com/andrewdalpino/UltraZoom"
