[project]
name = "census-table"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "wratnava@terpmail.umd.edu" }
]
requires-python = ">=3.5"
dependencies = []

[project.scripts]
census-table = "census_table:main"

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"
