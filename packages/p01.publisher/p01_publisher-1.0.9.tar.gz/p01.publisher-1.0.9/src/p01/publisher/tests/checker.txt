======
README
======

Let's test the source code with our custom checker:

  >>> from __future__ import absolute_import
  >>> import p01.checker
  >>> import p01.publisher

  >>> checker = p01.checker.Checker(p01.checker.CHECKERS)
  >>> checker.check(p01.publisher)
