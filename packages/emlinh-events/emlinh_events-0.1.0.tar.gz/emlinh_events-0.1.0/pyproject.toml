[project]
name = "emlinh-events"
version = "0.1.0"
description = "Shared event schemas for orchestrator messaging"
readme = ""
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Mike Trần", email = "hungmanh2607@gmail.com" }]
dependencies = ["pydantic>=2.6"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["emlinh_events*"]