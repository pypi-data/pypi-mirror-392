[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "soup_files"
version = "1.4.2"
authors = [
  { name="Bruno Chaves"},
]
description = "soup_files"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    
]

[tool.setuptools]
packages = [
  "soup_files",
  "soup_files.installer",
]
