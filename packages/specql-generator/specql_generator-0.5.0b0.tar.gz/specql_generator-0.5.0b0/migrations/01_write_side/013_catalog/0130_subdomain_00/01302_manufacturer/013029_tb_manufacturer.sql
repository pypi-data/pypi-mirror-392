-- ============================================================================
-- Table: catalog.tb_manufacturer
-- ============================================================================
-- [Table: MAN | Lists recognized printer/copier manufacturers (e.g., Canon, HP, Xerox), with internal identifiers and display attributes.]
-- ============================================================================

CREATE TABLE catalog.tb_manufacturer (
    -- ========================================================================
    -- Trinity Pattern: INTEGER primary key for performance
    -- ========================================================================
    pk_manufacturer INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,

    -- ========================================================================
    -- Trinity Pattern: UUID for stable public API
    -- ========================================================================
    id UUID DEFAULT gen_random_uuid() NOT NULL,
    -- ========================================================================
    -- Business Fields
    -- ========================================================================
    slug TEXT NOT NULL,
    name TEXT,
    abbreviation TEXT NOT NULL,
    color_name TEXT NOT NULL,
    -- ========================================================================
    -- Foreign Keys (Trinity Pattern: INTEGER references)
    -- ========================================================================

    -- ========================================================================
    -- Audit Fields (Trinity Pattern standard)
    -- ========================================================================
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    created_by UUID,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_by UUID,
    deleted_at TIMESTAMPTZ,
    deleted_by UUID,

    -- ========================================================================
    -- Constraints
    -- ========================================================================
    CONSTRAINT tb_manufacturer_id_key UNIQUE (id));

-- ============================================================================
-- Foreign Key Constraints (defined after table creation)
-- ============================================================================
-- ============================================================================
-- Documentation
-- ============================================================================
COMMENT ON TABLE catalog.tb_manufacturer IS
'Lists recognized printer/copier manufacturers (e.g., Canon, HP, Xerox), with internal identifiers and display attributes..

@fraiseql:type
trinity: true';

-- Trinity Pattern columns
COMMENT ON COLUMN catalog.tb_manufacturer.pk_manufacturer IS 'Internal INTEGER primary key used in joins and foreign keys.';
COMMENT ON COLUMN catalog.tb_manufacturer.id IS
'Public UUID identifier for external APIs and GraphQL.

@fraiseql:field
name: id
type: UUID!
required: true';
-- Business field columns (comments handled by CommentGenerator)

-- Foreign key columns (comments handled by CommentGenerator)

-- Audit field columns
COMMENT ON COLUMN catalog.tb_manufacturer.created_at IS 'Timestamp when the record was created.';
COMMENT ON COLUMN catalog.tb_manufacturer.created_by IS 'User or system who created the record.';
COMMENT ON COLUMN catalog.tb_manufacturer.updated_at IS 'Timestamp when the record was last updated.';
COMMENT ON COLUMN catalog.tb_manufacturer.updated_by IS 'User or system who last updated the record.';
COMMENT ON COLUMN catalog.tb_manufacturer.deleted_at IS 'Timestamp of soft deletion.';
COMMENT ON COLUMN catalog.tb_manufacturer.deleted_by IS 'User or system who deleted the record.';