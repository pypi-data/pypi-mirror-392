[project]
name = "bach-jsearch"
version = "1.0.2"
description = "RapidAPI: letscrape-6bRBa3QguO5/jsearch"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.25.0",
]

[project.scripts]
bach_jsearch = "server:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
]

[project.urls]
Homepage = "https://github.com/bachstudio/bach-jsearch"
Repository = "https://github.com/bachstudio/bach-jsearch"
Documentation = "https://github.com/bachstudio/bach-jsearch#readme"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.version]
path = "server.py"
pattern = '__version__ = "(?P<version>[^"]+)"'