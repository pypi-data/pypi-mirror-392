[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "quantumvitas"
version = "0.0.1"
description = "Placeholder package for QuantumVITAS (legacy GUI project)."
authors = [
  { name = "QMatSuite Developers" }
]
readme = "README.md"
requires-python = ">=3.8"
