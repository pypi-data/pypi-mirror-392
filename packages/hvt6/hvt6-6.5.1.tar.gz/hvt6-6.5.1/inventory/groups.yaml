---
global:
  data:
  domain: bvstv.lab

DNA Lab:
  data:
  ntp:
    servers:
      - ar.pool.ntp.org

CORE Lab:
  data:
  ntp:
    servers:
      - ar.pool.ntp.org

ACCESS Lab:
  data:
  ntp:
    servers:
      - ar.pool.ntp.org
