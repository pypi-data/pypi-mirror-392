Bifrost "TFBI_plage_2" run, but discarding all snapshot data (>100 MB... too large to include here).
Also discards EOS & rad tables (implied by tabparam.in) since they are >100 MB as well.

The mesh file is still included here because it's <1 MB.
