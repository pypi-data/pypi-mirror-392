<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ site_name }}</title>
    <style>{% include "style.css" %}</style>
    {% include "head_custom.html" ignore missing %}
  </head>
  <body>
    <div class="container">
      <h1>
        <a href="index.html"><img src="images/logo.png" height="72"></a>
        {{ site_name }}
      </h1>

      <h2>収録資料の一覧</h2>

      <div class="pb-3">
        {% for page in range(0, total_pages) %}
          <a href="index{{ page + 1 }}.html">index{{ page + 1 }}.html</a>
        {% endfor %}
      </div>

      <h2 >最近の登録</h2>

      <ul>
        {% for entry in entries %}
          <li>
            <a href="{{ entry.id }}/ro-crate-preview.html">
              {{ entry.id }}
              [{{ entry.type }}]
              {% for creator in entry.creator %}
                {% if creator["creator_name"]|length > 0 %}
                  {{ creator.creator_name[0].name }}
                {% endif %}
              {% endfor %}.
              {{ entry.title[0].title }}
            </a>

            {% if not entry.file is not defined %}
              {% for file in entry.file %}
                {% for date in file.date %}
                  {{ date["date_type"] }}:
                  {{ date["date"] }}
                {% endfor %}
              {% endfor %}
            {% endif %}
          </li>
        {% endfor %}
      </ul>

      <div>
        最終更新日: {{ now }}
        <a href="resourcelist.xml">ResourceSync XML</a>
      </div>

      {% include "footer.j2" %}
    </div>
  </body>
</html>
