<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ site_name }}</title>
    <style>{% include "style.css" %}</style>
    {% include "head_custom.html" ignore missing %}
  </head>
  <body>
    <div class="container">
      <p>
        <a href="./index.html"><img src="./images/logo.png" height="48"></a>
        <a href="./index.html">{{ site_name }}</a>
      </p>

      <h2>収録資料の一覧</h2>

      <p>{{ (page - 1) * per_page + 1 }}件目から{{ page * per_page }}件目までの登録を表示しています。</p>

      <ul>
        {% for entry in entries %}
          <li>
            <a href="{{ entry.id }}/ro-crate-preview.html">
              {{ entry.id }}
              [{{ entry.type }}]
              {% for creator in entry.creator %}
                {% if creator["creator_name"]|length > 0 %}
                  {{ creator.creator_name[0].name }}
                {% endif %}
              {% endfor %}.
              {{ entry.title[0].title }}
            </a>

            {% if not entry.file is not defined %}
              {% for file in entry.file %}
                {% for date in file.date %}
                  {{ date["date_type"] }}:
                  {{ date["date"] }}
                {% endfor %}
              {% endfor %}
            {% endif %}
          </li>
        {% endfor %}
      </ul>

      <div>
        <a href="index.html">トップページ</a>

        {% if previous_page %}
          <a href="index{{ previous_page }}.html">前のページ</a>
        {% endif %}

        {% if next_page %}
          <a href="index{{ next_page }}.html">次のページ</a>
        {% endif %}
      </div>

      <div>
        最終更新日: {{ now }}
        <a href="resourcelist.xml">ResourceSync XML</a>
      </div>

      {% include "footer.j2" %}
    </div>
  </body>
</html>
