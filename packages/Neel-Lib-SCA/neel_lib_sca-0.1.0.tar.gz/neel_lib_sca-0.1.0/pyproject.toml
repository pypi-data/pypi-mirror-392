[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Neel_Lib_SCA"
version = "0.1.0"
description = "Supply Chain analytics library for Django and Python projects."
authors = [{ name = "Neel Sawant", email = "neel.sawant09@gmail.com" }]
requires-python = ">=3.9"
dependencies = ["Django>=4.0"]
keywords = ["eoq", "django", "analysis", "forecasting", "products"]
