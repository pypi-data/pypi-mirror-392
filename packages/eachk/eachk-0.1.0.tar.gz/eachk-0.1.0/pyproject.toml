[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eachk"
version = "0.1.0"
description = "A package for skipping every k-th iteration"
readme = "readme.md"
authors = [{ name="Alexa Korabelnikov", email="you@example.com" }]
license = { text = "MIT" }
dependencies = []
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]
requires-python = ">=3.6"
