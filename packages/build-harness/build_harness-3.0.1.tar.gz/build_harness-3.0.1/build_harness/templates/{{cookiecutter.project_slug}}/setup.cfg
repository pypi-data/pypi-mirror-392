[behave]
junit=true
junit_directory=dist/behave
snippets=false

[mutmut]
backup=False
dict_synonyms=Struct, NamedStruct
paths_to_mutate={{cookiecutter.project_slug}}/
runner=python -m pytest
tests_dir=tests/

[mypy]

[pydocstyle]
convention = pep257
match_dir = (?<!\.)(?<!\venv)(?!tests).*
