[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mat_eval"
version = "0.0.1.dev0"  # 改成占位版本
description = "AI for Materials Evaluation Harness - Our library for reproducible and transparent evaluation of AI for Accelerated Materials Discovery."
authors = [{name = "Your Name", email = "your@email.com"}]  # 加上作者信息
requires-python = ">= 3.10"
dependencies = []  # 暂时清空依赖