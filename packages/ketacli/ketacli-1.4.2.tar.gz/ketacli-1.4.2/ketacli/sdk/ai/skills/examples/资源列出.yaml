name: 资源列出
summary: 列出指定仓库中的资源，包括指标、事件、日志等
enabled: true
triggers: [资源列出, 资源, 列出]
permissions: [read_only]
tools_whitelist: [list_assets, list_queryable]
description: |
  ## 资源列出步骤
    1. 通过工具 `list_queryable`来列出所有可用的资源类型。
    2. 通过工具 `list_assets type=<资源类型> prefix=<名称关键字>` 来列出指定类型的资源。
