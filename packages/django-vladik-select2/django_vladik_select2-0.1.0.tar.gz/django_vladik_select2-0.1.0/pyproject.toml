[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "django-vladik-select2"
version = "0.1.0"
description = "Select2 para Django con dependencias dinámicas y re-init en modales"
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
authors = [
  {name = "Vladik Muñoz", email = "vladikma87@gmail.com"}
]

dependencies = [
  "Django>=3.2",
  "django-widget-tweaks",
]

[project.urls]
Homepage = "https://github.com/vladikma/django-vladik-select2"
