### Example .env for the fastAppear project
# Copy this file to `.env` in your deployment or local environment and update values

# Database connection (required)
# Example for PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DB_URI=postgresql://fastappear_user:strongpassword@localhost:5432/fastappear_db

# Project root path (optional)
# Set to a static base path if required by your deployment
ROOT_PATH=

# Logging level (defaults to INFO if not provided)
LOGGING_LEVEL=INFO

# JWT settings
# Use a securely generated secret key. Do not commit this value to source control.
JWT_SECRET_KEY=replace_with_a_secure_random_key
JWT_ALGORITHM=HS512
# Access token expiry in minutes (defaults to 720)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=720
# Refresh token expiry in days (defaults to 7)
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Additional tips:
# - Use a proper secrets manager for production (AWS Secrets Manager, HashiCorp Vault, etc.)
# - For local development you can use a small password and a local DB, but never reuse production secrets.
