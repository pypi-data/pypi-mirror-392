[build-system]
requires = ["hatchling>=1.27.0,<2.0.0"]
build-backend = "hatchling.build"

[project]
name = "ndc-types"
version = "0.1.0"
dependencies = [
    "pydantic>=2.12.4,<3.0.0",
    "nvidiautils==0.1.0,<0.2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/ndc"]

[tool.hatch.build.targets.sdist]
packages = ["src/ndc"]
