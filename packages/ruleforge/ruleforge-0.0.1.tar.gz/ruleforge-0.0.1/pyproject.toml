[build-system]
requires = ["setuptools>=61.0", "cython"]
build-backend = "setuptools.build_meta"

[project]
name = "ruleforge"
version = "0.0.1"
description = "Very easy pop and play package to make pre-processing easier than before ;)"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

authors = [
    { name = "Akash K Tesla" }
]

dependencies = [
    "pandas"
]

