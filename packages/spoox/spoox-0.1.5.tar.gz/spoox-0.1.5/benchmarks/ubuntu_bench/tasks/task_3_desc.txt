I have created a python project.
I want to include the official python gitignore template, available in the /github/gitignore repository.
Do not simply overwrite the existing gitignore, instead, merge its entries and avoid duplications.