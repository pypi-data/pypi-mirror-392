Analyze the SSH authentication log. If any IP has more than 100 failed attempts,
write that IP into a file called banned_ips.txt in the home directory. Only write one IP per line.
Also configure a cron job that runs every 5 minutes and overwrites this file based on the latest logs.