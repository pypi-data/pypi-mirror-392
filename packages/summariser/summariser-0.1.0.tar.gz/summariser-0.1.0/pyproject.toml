[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "summariser"
version = "0.1.0"
description = "Minimal text summarization helper using HuggingFace transformers."
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [
  { name = "Parth Zanwar", email = "parthzanwr1509@gmail.com" }
]

dependencies = [
    "transformers>=4.0",
    "torch"
]