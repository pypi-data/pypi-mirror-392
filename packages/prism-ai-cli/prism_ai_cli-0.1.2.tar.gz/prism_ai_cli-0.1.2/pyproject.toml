[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "prism-ai-cli"
version = "0.1.2"
description = "Prism AI CLI — Next-gen AI infrastructure"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "Your Name", email = "you@prism.ai"}]

[project.urls]
Homepage = "https://prism.ai"

[project.scripts]
prism = "prism.__main__:run_prism"

[tool.setuptools]
packages = ["prism"]
include-package-data = true