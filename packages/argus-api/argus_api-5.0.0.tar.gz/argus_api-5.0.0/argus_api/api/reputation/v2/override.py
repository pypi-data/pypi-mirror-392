"""Autogenerated API"""

from argus_api.session import _legacy_session as session
import logging
from argus_cli.plugin import register_command
from argus_plugins import argus_cli_module

log = logging.getLogger(__name__)


@register_command(extending=("reputation", "v2", "override"), module=argus_cli_module)
def add_override(
    list: str,
    reason: str,
    score: float,
    type: str,
    validUntil: int,
    value: str,
    applyToSubdomains: bool = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    r"""Add a new reputation override (DEV)

    :param str list: ID or shortName of the override List the override will be part of
    :param str reason: Why the override was created
    :param float score: Reputation score set on observations matching the override\'s type and value
    :param str type: Which Indicator Type the value field will be interpreted as. Available types listed in \'reputation\/v2\/type\' endpoint
    :param int validUntil: When the override expires. Use 0 for no expiration
    :param str value: What to override. Value must be valid according to the type
    :param bool applyToSubdomains: Whether the override also applies to its subdomains. Only for overrides of type \'domain\'
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes

    :returns: dictionary translated from JSON
    """

    route = "/reputation/v2/override".format()
    headers = {}

    body = body or {}
    # Only send applyToSubdomains if the argument was provided, don't send null values
    if applyToSubdomains is not None:
        body.update({"applyToSubdomains": applyToSubdomains})
    # Only send list if the argument was provided, don't send null values
    if list is not None:
        body.update({"list": list})
    # Only send reason if the argument was provided, don't send null values
    if reason is not None:
        body.update({"reason": reason})
    # Only send score if the argument was provided, don't send null values
    if score is not None:
        body.update({"score": score})
    # Only send type if the argument was provided, don't send null values
    if type is not None:
        body.update({"type": type})
    # Only send validUntil if the argument was provided, don't send null values
    if validUntil is not None:
        body.update({"validUntil": validUntil})
    # Only send value if the argument was provided, don't send null values
    if value is not None:
        body.update({"value": value})

    log.debug(
        "POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or "")
    )

    response = session.post(
        route,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response


@register_command(extending=("reputation", "v2", "override"), module=argus_cli_module)
def delete_override(
    id: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
) -> dict:
    r"""Delete an existing reputation override (DEV)

    :param str id: ID of the override to delete
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes

    :returns: dictionary translated from JSON
    """

    route = "/reputation/v2/override/{id}".format(id=id)
    headers = {}

    log.debug("DELETE %s (headers: %s)" % (route, str(headers)))

    response = session.delete(
        route,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response


@register_command(extending=("reputation", "v2", "override"), module=argus_cli_module)
def get_override(
    id: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
) -> dict:
    r"""Fetch an existing reputation override (DEV)

    :param str id: ID of the override
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes

    :returns: dictionary translated from JSON
    """

    route = "/reputation/v2/override/{id}".format(id=id)
    headers = {}

    log.debug("GET %s (headers: %s)" % (route, str(headers)))

    response = session.get(
        route,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response


@register_command(extending=("reputation", "v2", "override"), module=argus_cli_module)
def list_overrides(
    sortBy: str = None,
    keywords: str = None,
    keywordFieldStrategy: str = None,
    limit: int = 25,
    keywordMatchStrategy: str = "all",
    offset: int = 0,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
) -> dict:
    r"""Simple search for overrides (DEV)

    :param list sortBy: How to sort the results. Sortable values are listed in the reputation\/v2\/override\/search endpoint
    :param list keywords: Keywords to search for.
    :param list keywordFieldStrategy: Which fields to search for keywords in.
    :param int limit: Number of overrides to return.
    :param str keywordMatchStrategy: Whether all or any of the keywords are required to match any of the fields.
    :param int offset: Number of overrides to skip.
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes

    :returns: dictionary translated from JSON
    """

    route = "/reputation/v2/override".format()
    headers = {}

    query_parameters = {}
    # Only send limit if the argument was provided, don't send null values
    if limit is not None:
        query_parameters.update({"limit": limit})
    # Only send keywordMatchStrategy if the argument was provided, don't send null values
    if keywordMatchStrategy is not None:
        query_parameters.update({"keywordMatchStrategy": keywordMatchStrategy})
    # Only send offset if the argument was provided, don't send null values
    if offset is not None:
        query_parameters.update({"offset": offset})
    # Only send sortBy if the argument was provided, don't send null values
    if sortBy is not None:
        query_parameters.update({"sortBy": sortBy})
    # Only send keywords if the argument was provided, don't send null values
    if keywords is not None:
        query_parameters.update({"keywords": keywords})
    # Only send keywordFieldStrategy if the argument was provided, don't send null values
    if keywordFieldStrategy is not None:
        query_parameters.update({"keywordFieldStrategy": keywordFieldStrategy})

    log.debug("GET %s (headers: %s)" % (route, str(headers)))

    response = session.get(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response


@register_command(extending=("reputation", "v2", "override"), module=argus_cli_module)
def search_overrides(
    domainSearch: dict = None,
    endTimestamp: str = None,
    excludeFlags: str = None,
    includeFlags: str = None,
    ipSearch: dict = None,
    keywords: str = None,
    list: str = None,
    sortBy: str = None,
    startTimestamp: str = None,
    type: str = None,
    includeDeleted: bool = False,
    includeExpired: bool = False,
    keywordFieldStrategy: str = None,
    keywordMatchStrategy: str = "any",
    limit: int = 25,
    maxScore: float = 1.0,
    minScore: float = 0.0,
    offset: int = 0,
    timeFieldStrategy: str = None,
    timeMatchStrategy: str = "all",
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    r"""Search for overrides (DEV)

    :param dict domainSearch:
    :param str endTimestamp: Restrict search to data before this time \(epoch millis\) according to selected TimeFieldStrategies. Allows unix timestamp \(milliseconds\)\, ISO timestamp\, or relative time specifies. See https\:\/\/docs.mnemonic.no\/x\/AQDXAQ  Allows to specify a unix timestamp \(milliseconds\)\, ISO timestamp\, or relative time.
    :param list excludeFlags: Require overrides to not have certain flags set.
    :param list includeFlags: Require overrides to have certain flags set.
    :param dict ipSearch:
    :param list keywords: Keywords to search for in fields specified by keywordFieldStrategy.
    :param list list: IDs or shortNames of Override Lists to search in
    :param list sortBy: Specify the order of results. Prefix with \'\-\' to order descending.
    :param str startTimestamp: Restrict search to data after this time \(epoch millis\) according to selected TimeFieldStrategies. Allows unix timestamp \(milliseconds\)\, ISO timestamp\, or relative time specifies. See https\:\/\/docs.mnemonic.no\/x\/AQDXAQ  Allows to specify a unix timestamp \(milliseconds\)\, ISO timestamp\, or relative time.
    :param list type: Types of override to search for
    :param bool includeDeleted: Whether to include deleted search results.
    :param bool includeExpired: Whether to include expired overrides
    :param list keywordFieldStrategy: Which fields to search for keywords in.
    :param str keywordMatchStrategy: Whether all or any of the keywords are required to match any of the fields.
    :param int limit: Number of search results to return.
    :param float maxScore: Maximum score
    :param float minScore: Minimum score
    :param int offset: Number of search results to skip.
    :param list timeFieldStrategy: Fields to match timestamps on
    :param str timeMatchStrategy: Whether any or all fields in timeFieldStrategy needs to fall inside the start and end timestamps
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes

    :returns: dictionary translated from JSON
    """

    route = "/reputation/v2/override/search".format()
    headers = {}

    body = body or {}
    # Only send domainSearch if the argument was provided, don't send null values
    if domainSearch is not None:
        body.update({"domainSearch": domainSearch})
    # Only send endTimestamp if the argument was provided, don't send null values
    if endTimestamp is not None:
        body.update({"endTimestamp": endTimestamp})
    # Only send excludeFlags if the argument was provided, don't send null values
    if excludeFlags is not None:
        body.update({"excludeFlags": excludeFlags})
    # Only send includeDeleted if the argument was provided, don't send null values
    if includeDeleted is not None:
        body.update({"includeDeleted": includeDeleted})
    # Only send includeExpired if the argument was provided, don't send null values
    if includeExpired is not None:
        body.update({"includeExpired": includeExpired})
    # Only send includeFlags if the argument was provided, don't send null values
    if includeFlags is not None:
        body.update({"includeFlags": includeFlags})
    # Only send ipSearch if the argument was provided, don't send null values
    if ipSearch is not None:
        body.update({"ipSearch": ipSearch})
    # Only send keywordFieldStrategy if the argument was provided, don't send null values
    if keywordFieldStrategy is not None:
        body.update({"keywordFieldStrategy": keywordFieldStrategy})
    # Only send keywordMatchStrategy if the argument was provided, don't send null values
    if keywordMatchStrategy is not None:
        body.update({"keywordMatchStrategy": keywordMatchStrategy})
    # Only send keywords if the argument was provided, don't send null values
    if keywords is not None:
        body.update({"keywords": keywords})
    # Only send limit if the argument was provided, don't send null values
    if limit is not None:
        body.update({"limit": limit})
    # Only send list if the argument was provided, don't send null values
    if list is not None:
        body.update({"list": list})
    # Only send maxScore if the argument was provided, don't send null values
    if maxScore is not None:
        body.update({"maxScore": maxScore})
    # Only send minScore if the argument was provided, don't send null values
    if minScore is not None:
        body.update({"minScore": minScore})
    # Only send offset if the argument was provided, don't send null values
    if offset is not None:
        body.update({"offset": offset})
    # Only send sortBy if the argument was provided, don't send null values
    if sortBy is not None:
        body.update({"sortBy": sortBy})
    # Only send startTimestamp if the argument was provided, don't send null values
    if startTimestamp is not None:
        body.update({"startTimestamp": startTimestamp})
    # Only send timeFieldStrategy if the argument was provided, don't send null values
    if timeFieldStrategy is not None:
        body.update({"timeFieldStrategy": timeFieldStrategy})
    # Only send timeMatchStrategy if the argument was provided, don't send null values
    if timeMatchStrategy is not None:
        body.update({"timeMatchStrategy": timeMatchStrategy})
    # Only send type if the argument was provided, don't send null values
    if type is not None:
        body.update({"type": type})

    log.debug(
        "POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or "")
    )

    response = session.post(
        route,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response


@register_command(extending=("reputation", "v2", "override"), module=argus_cli_module)
def update_override(
    id: str,
    applyToSubdomains: bool = None,
    reason: str = None,
    score: float = None,
    validUntil: int = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    r"""Update an existing reputation override (DEV)

    :param str id: ID of the override to update
    :param bool applyToSubdomains: Whether the Override also applies to its subdomains. Only for Overrides of domain type
    :param str reason: Why the Override was created
    :param float score: Reputation value set on Observations matching the type and value. Must be between 0.0 and 1.0
    :param int validUntil: When the Override expires. Use 0 for no expiration
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes

    :returns: dictionary translated from JSON
    """

    route = "/reputation/v2/override/{id}".format(id=id)
    headers = {}

    body = body or {}
    # Only send applyToSubdomains if the argument was provided, don't send null values
    if applyToSubdomains is not None:
        body.update({"applyToSubdomains": applyToSubdomains})
    # Only send reason if the argument was provided, don't send null values
    if reason is not None:
        body.update({"reason": reason})
    # Only send score if the argument was provided, don't send null values
    if score is not None:
        body.update({"score": score})
    # Only send validUntil if the argument was provided, don't send null values
    if validUntil is not None:
        body.update({"validUntil": validUntil})

    log.debug("PUT %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.put(
        route,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response
