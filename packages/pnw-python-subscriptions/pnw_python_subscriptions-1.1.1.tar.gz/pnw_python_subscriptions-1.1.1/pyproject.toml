[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pnw-python-subscriptions"
version = "1.1.1"
description = "PNW subscription utility"
readme = "README.md"
requires-python = ">=3.7"
authors = [{name = "Sumnor"}]
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["pnw_subscriptions*"]
