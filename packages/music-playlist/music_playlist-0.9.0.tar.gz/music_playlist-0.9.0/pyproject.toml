[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "music-playlist"
version = "0.9.0"
description = "Music for the masses"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["extract_music"]
