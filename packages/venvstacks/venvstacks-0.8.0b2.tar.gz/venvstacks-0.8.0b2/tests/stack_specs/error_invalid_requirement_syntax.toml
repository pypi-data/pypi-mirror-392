# Ensure suitable error is raised for invalid requirements syntax

[[runtimes]]
name = "cpython-3.11"
python_implementation = "cpython@3.11.11"
requirements = [
    # We're not fully testing the packaging requirements validator itself,
    # just that the validator is called when the stack spec is parsed
    "name<>"
]
