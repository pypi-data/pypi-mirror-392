[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quiz-cultura-general"
version = "1.0.0"
description = "Quiz interactivo de cultura general"
readme = "README.md"
authors = [{ name = "Patricia Elena Paun" }]
license = { text = "MIT" }
requires-python = ">=3.6"

[project.scripts]
quiz-cultura = "quiz_cultura.main:main"