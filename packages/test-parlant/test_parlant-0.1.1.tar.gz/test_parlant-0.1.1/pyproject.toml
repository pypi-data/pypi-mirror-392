[project]
name = "test-parlant"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "anyio>=4.11.0", "fastapi>=0.121.2", "pytest>=9.0.1", "pytest-asyncio>=1.3.0", "pytest-tornasync>=0.6.0.post2", "toml>=0.10.2", "uvicorn>=0.38.0",]

[tool.setuptools.package-data]
test_parlant = [ "config.yaml",]

[tool.pytest.ini_options]
testpaths = [ "tests",]
pythonpath = [ "src",]
