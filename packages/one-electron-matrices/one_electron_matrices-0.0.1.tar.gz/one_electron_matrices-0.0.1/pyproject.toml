[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "one_electron_matrices"
version = "0.0.1"
authors = [
  { name="Stiv Llenga", email="stivllenga@gmail.com" },
]
description = "Code responsible for generating the one-electron integrals."
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/grynova-ccc/quantum_periodic_representations"
