[project]
name = "terminator-pypi-pruner-demo"
version = "0.0.3"
description = "Placeholder package to exercise PyPI cleanup workflow"
authors = [{ name = "Your Name", email = "you@example.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"
