[project]
name = "mirjalolspeed"
version = "0.1.0"
description = "Live internet speed test library by Mirjalol"
authors = [
    { name="Mirjalol" }
]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
