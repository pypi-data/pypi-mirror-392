[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zangshencc"                # 发布到 PyPI 的名字（要唯一）
version = "0.1.1"
description = "zangshencc"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name = "cc", email = "zangshencc@gmail.com" }
]
dependencies = [
    "DrissionPage>=4.0",
]