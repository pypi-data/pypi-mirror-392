[project]
name = "arcbeam-langchain-connector"
version = "0.0.5-rc1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Arcbeam Eng", email = "eng@arcbeam.ai" }
]
requires-python = ">=3.13"
dependencies = [
    "langchain>=0.3.27",
]

[project.scripts]
arcbeam-langchain-connector = "arcbeam_langchain_connector:main"

[build-system]
requires = ["uv_build>=0.8.18,<0.9.0"]
build-backend = "uv_build"

[tool.ruff]
lint.select = ["I"]
line-length = 120
