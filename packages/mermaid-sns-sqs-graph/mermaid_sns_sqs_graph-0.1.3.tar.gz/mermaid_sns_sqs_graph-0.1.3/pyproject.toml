[project]
name = "mermaid-sns-sqs-graph"
version = "0.1.3"
description = "Utility to map the sns sqs connections"
requires-python = ">=3.8"
readme = "README.md"
authors = [
  { name = "Gaurav soni" }
]

dependencies = [
    "boto3>=1.40.66",
]

[project.scripts]
mermaid-sns-sqs-graph = "mermaid_sns_sqs_graph.main:main"

