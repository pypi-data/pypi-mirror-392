[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pachinko-sdlr-dv"
version = "0.1.6"
description = "Simulador de Pachinko con modo fiebre y números aleatorios."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
authors = [{name = "Daniel Vega"}, {name = "Sergio de la Rubia"}]

[tool.setuptools.packages.find]
where = ["."]