[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py-gnuplot"
version = "1.3.2"
description='py-gnuplot is a python plot tools based on gnuplot.'
readme = "README.rst"
dependencies = [
    "pandas"
]
authors = [
    {name = "Yongping Guo", email = "guoyoooping@163.com"}
]
keywords = ["gnuplot", "pandas", "py-gnuplot"]
license = {text = "GPLv3"}

[project.urls]
#"Homepage" = 'https://github.com/guoyoooping/networkdevice'
