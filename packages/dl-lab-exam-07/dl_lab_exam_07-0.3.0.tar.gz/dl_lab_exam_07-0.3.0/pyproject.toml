[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dl-lab-exam-07"   # ✅ choose a unique name (only letters, numbers, - or _)
version = "0.3.0"
description = "Deep learning lab project"
authors = [
  { name="lilly", email="reddylilly013@gmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
