[project]
name = "simplemathy"        # Must be unique on PyPI
version = "0.1.0"
description = "A tiny example package for learning PyPI publishing."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

[project.urls]
Homepage = "https://pypi.org/project/simplemathy/"
