---
title: "sup workspace"
description: "Manage workspaces"
---

Manage workspaces

## Usage

```bash
sup workspace [COMMAND] [OPTIONS]
```

## Subcommands

| Command | Description |
|---------|-------------|
| `sup workspace list` | List all available workspaces |
| `sup workspace use` | Set the default workspace for current session |
| `sup workspace info` | Show detailed information about a workspace |
| `sup workspace set-target` | Set the import target workspace for cross-workspace operations |
| `sup workspace show` | Show current workspace context including source and import target |

## Examples

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs>
  <TabItem label="List workspaces">
    ```bash
    sup workspace list
    ```
  </TabItem>
  <TabItem label="Set active workspace">
    ```bash
    sup workspace use 123
    ```
  </TabItem>
</Tabs>
