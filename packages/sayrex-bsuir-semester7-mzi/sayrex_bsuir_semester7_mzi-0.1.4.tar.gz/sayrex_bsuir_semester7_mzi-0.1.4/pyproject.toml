[build-system]
requires = ["setuptools>=77.0.3","wheel","build"]
build-backend = "setuptools.build_meta"

[project]
name = "sayrex-bsuir-semester7-mzi"
version = "0.1.4"
description = "Лабораторные работы по МЗИ (7 семестр)"
readme = "README.md"
authors = [{ name="sayrex", email="reshetneff2013@gmail.com" }]
license = "MIT"
requires-python = ">=3.6"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = ["numpy"]