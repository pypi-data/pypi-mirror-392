[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auth_sso_client"
version = "0.1.2"
description = "Auth server WeCom SSO client for Python/FastAPI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "fastapi>=0.116.1",
]

[dependency-groups]
dev = [
    "build>=1.3.0",
]
