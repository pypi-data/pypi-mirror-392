[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "leo-sat-sim"
version = "0.1.0"
authors = [
    { name="K" }
]
description = "A 3D satellite simulator web app."
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.8"

dependencies = [
    "flask",
    "os"
]

[project.scripts]
yourproject = "leo_sat_sim.cli:main"
