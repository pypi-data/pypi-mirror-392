[project]
name = "django_ulidx"
version = "1.0.9"
description = ""
authors = [{ name = "Poetry User", email = "pypi.clock071@passinbox.com" }]
readme = "README.md"
license = "MIT"
package-mode = true
packages = [
    {include = "django_ulidx/__init__.py"},
    {include = "django_ulidx/models.py"},
]

requires-python = ">=3.13"

dependencies = [
    "django>=5.2.8",
    "python-ulid>=3.1.0",
]


[tool.poetry.group.dev.dependencies]
mysqlclient = "^2.2.7"
psycopg = {extras = ["binary", "pool"], version = "^3.2.12"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
