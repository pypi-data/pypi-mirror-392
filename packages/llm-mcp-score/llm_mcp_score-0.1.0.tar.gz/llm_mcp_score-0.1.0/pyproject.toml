[project]
name = "llm_mcp_score"
version = "0.1.0"
description = "A minimal MCP service for testing LLM understanding and invocation of MCP tools"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "mcp[server]>=1.2.0"
]

[project.urls]
"Homepage" = "https://github.com/yourusername/llm_mcp_score"

[project.scripts]
llm-mcp-score = "llm_mcp_score.server:main"
