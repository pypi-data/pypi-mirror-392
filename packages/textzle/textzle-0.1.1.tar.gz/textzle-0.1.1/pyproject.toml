[project]
name = "textzle"
version = "0.1.1"
description = "This is the base engine for Textzle, a text adventure game."
authors = [
    {name = "splot.dev"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.poetry]
packages = [{ include = "textzle" }]
keywords = []

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

