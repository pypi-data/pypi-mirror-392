asyncapi: "3.0.0"
info:
  title: Server part of the Ping service example
  version: 1.0.0

operations:
  /application/ping:
    action: receive
    channel: { $ref: "./common.asyncapi.yaml#/channels/~1ping~1pubsub" }
