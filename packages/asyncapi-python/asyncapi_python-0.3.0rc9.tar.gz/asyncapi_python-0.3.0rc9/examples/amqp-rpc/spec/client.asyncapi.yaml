asyncapi: "3.0.0"
info:
  title: Client part of the Ping-Pong service example
  version: 1.0.0

operations:
  pingRequest:
    action: send
    channel: { $ref: "./common.asyncapi.yaml#/channels/ping" }
    reply:
      channel: { $ref: "./common.asyncapi.yaml#/channels/pong" }
