[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "emotionx"
version = "0.0.1"

description = "A simple text emotion detection tool based on keyword matching."
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT License"}
authors = [
    { name="Shawn Mendes", email="example@email.com" }
]

[project.urls]
Homepage = "https://pypi.org/project/textemotionx/"
