/* General Styles */
QWidget {
    background-color: #1E4C2E;
    color: #F4F1F0;
    border: none; /* Removed global border for cleaner look */
}

/* Buttons */
QPushButton {
    background-color: #75BE77;
    color: #1E4C2E; /* Changed to dark green for contrast */
    border: 1px solid #6F4631;
    padding: 5px;
    border-radius: 3px;
}

QPushButton:hover {
    background-color: #F8EC9A;
}

QPushButton:pressed {
    background-color: #6F4631;
}

/* LineEdit and TextEdit */
QLineEdit, QTextEdit {
    background-color: #F8EC9A;
    color: #1E4C2E; /* Changed to dark green for readability */
    border: 1px solid #6F4631;
    padding: 3px;
    border-radius: 3px;
    selection-background-color: #6F4631; /* Adjusted for better contrast */
    selection-color: #F4F1F0; /* Adjusted for better contrast */
}

/* Labels */
QLabel {
    color: #F4F1F0;
}

/* GroupBox */
QGroupBox {
    background-color: #1E4C2E;
    border: 1px solid #6F4631;
    margin-top: 1ex; /* leave space at the top for the title */
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top center; /* Changed from top left to top center for better aesthetics */
    padding: 0 3px;
    background-color: #75BE77;
    color: #1E4C2E; /* Changed to dark green for contrast */
}

/* ComboBox */
QComboBox {
    background-color: #F8EC9A;
    color: #1E4C2E; /* Changed to dark green for contrast */
    border: 1px solid #6F4631;
    padding: 1px 0px 1px 3px; /* top, right, bottom, left */
    border-radius: 3px;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 15px;
    border-left-width: 1px;
    border-left-color: #6F4631;
    border-left-style: solid; /* just a single line */
    border-top-right-radius: 3px; /* same radius as the QComboBox */
    border-bottom-right-radius: 3px;
}

QComboBox::down-arrow {
    image: none; /* Removed image dependency */
    border-left: 5px solid transparent;
    border-right: 5px solid transparent;
    border-top: 6px solid #1E4C2E; /* Custom arrow using borders, dark green color */
    width: 0px;
    height: 0px;
    margin: 0 auto;
}

/* ScrollBar */
QScrollBar:vertical {
    border: 1px solid #6F4631;
    background: #1E4C2E;
    width: 10px;
    margin: 15px 0 15px 0;
}

QScrollBar::handle:vertical {
    background: #75BE77;
    min-height: 20px;
}

QScrollBar::add-line:vertical {
    border: 1px solid #6F4631;
    background: #F8EC9A;
    height: 15px;
    subcontrol-origin: margin; /* Changed from bottom to margin for consistency */
    subcontrol-position: bottom;
}

QScrollBar::sub-line:vertical {
    border: 1px solid #6F4631;
    background: #F8EC9A;
    height: 15px;
    subcontrol-origin: margin; /* Changed from top to margin for consistency */
    subcontrol-position: top;
}

QScrollBar::up-arrow:vertical, QScrollBar::down-arrow:vertical {
    background: #1E4C2E; /* Changed to dark green */
}

QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    background: none;
}

/* CheckBox */
QCheckBox {
    color: #F4F1F0;
}

QCheckBox::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #6F4631;
    background-color: #F8EC9A;
}

QCheckBox::indicator:checked {
    background-color: #75BE77;
}

/* RadioButton */
QRadioButton {
    color: #F4F1F0;
}

QRadioButton::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #6F4631;
    border-radius: 6px; /* Makes it circular */
    background-color: #F8EC9A;
}

QRadioButton::indicator:checked {
    background-color: #75BE77;
}

/* TabWidget */
QTabWidget::pane {
    border: 1px solid #6F4631;
    background-color: #1E4C2E;
}

QTabBar::tab {
    background: #F8EC9A;
    border: 1px solid #6F4631;
    border-bottom: none; /* Removed border-bottom-color for cleaner look */
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    min-width: 8ex;
    padding: 2px;
    color: #1E4C2E; /* Changed to dark green for readability */
}

QTabBar::tab:selected, QTabBar::tab:hover {
    background: #75BE77;
}

QTabBar::tab:selected {
    border-color: #6F4631;
    border-bottom-color: #75BE77; /* make the selected tab appear above the pane */
}

/* MenuBar */
QMenuBar {
    background-color: #F8EC9A;
    color: #1E4C2E; /* Changed to dark green for readability */
}

QMenuBar::item {
    spacing: 3px;
    padding: 1px 4px;
    background: transparent;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background: #75BE77;
}

QMenuBar::item:pressed {
    background: #6F4631;
}

/* Menu */
QMenu {
    background-color: #F8EC9A;
    border: 1px solid #6F4631;
    border-radius: 3px;
}

QMenu::item {
    padding: 2px 25px 2px 20px;
    border: 1px solid transparent; /* reserve space for selection border */
}

QMenu::item:selected {
    background-color: #75BE77;
    color: #F4F1F0; /* Changed to light color for readability */
}

QMenu::separator {
    height: 1px;
    background: #6F4631;
    margin-left: 10px;
    margin-right: 10px;
}