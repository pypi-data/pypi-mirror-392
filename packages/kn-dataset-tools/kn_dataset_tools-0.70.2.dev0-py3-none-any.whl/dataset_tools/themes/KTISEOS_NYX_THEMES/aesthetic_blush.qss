/* General Styles */
QWidget {
    background-color: #503836;
    color: #FAFAFA;
    border: none; /* Removed global border for cleaner look */
}

/* Buttons */
QPushButton {
    background-color: #BAB9BF;
    color: #503836; /* Changed to dark brown for contrast */
    border: 1px solid #52544C;
    padding: 5px;
    border-radius: 3px;
}

QPushButton:hover {
    background-color: #887579;
}

QPushButton:pressed {
    background-color: #52544C;
}

/* LineEdit and TextEdit */
QLineEdit, QTextEdit {
    background-color: #887579;
    color: #FAFAFA; /* Changed to white for readability */
    border: 1px solid #52544C;
    padding: 3px;
    border-radius: 3px;
    selection-background-color: #52544C; /* Adjusted for better contrast */
    selection-color: #FAFAFA; /* Adjusted for better contrast */
}

/* Labels */
QLabel {
    color: #FAFAFA;
}

/* GroupBox */
QGroupBox {
    background-color: #503836;
    border: 1px solid #52544C;
    margin-top: 1ex; /* leave space at the top for the title */
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top center; /* Changed from top left to top center for better aesthetics */
    padding: 0 3px;
    background-color: #BAB9BF;
    color: #503836; /* Changed to dark brown for contrast */
}

/* ComboBox */
QComboBox {
    background-color: #887579;
    color: #FAFAFA; /* Changed to white for readability */
    border: 1px solid #52544C;
    padding: 1px 0px 1px 3px; /* top, right, bottom, left */
    border-radius: 3px;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 15px;
    border-left-width: 1px;
    border-left-color: #52544C;
    border-left-style: solid; /* just a single line */
    border-top-right-radius: 3px; /* same radius as the QComboBox */
    border-bottom-right-radius: 3px;
}

QComboBox::down-arrow {
    image: none; /* Removed image dependency */
    border-left: 5px solid transparent;
    border-right: 5px solid transparent;
    border-top: 6px solid #FAFAFA; /* Custom arrow using borders, white color */
    width: 0px;
    height: 0px;
    margin: 0 auto;
}

/* ScrollBar */
QScrollBar:vertical {
    border: 1px solid #52544C;
    background: #503836;
    width: 10px;
    margin: 15px 0 15px 0;
}

QScrollBar::handle:vertical {
    background: #BAB9BF;
    min-height: 20px;
}

QScrollBar::add-line:vertical {
    border: 1px solid #52544C;
    background: #887579;
    height: 15px;
    subcontrol-origin: margin; /* Changed from bottom to margin for consistency */
    subcontrol-position: bottom;
}

QScrollBar::sub-line:vertical {
    border: 1px solid #52544C;
    background: #887579;
    height: 15px;
    subcontrol-origin: margin; /* Changed from top to margin for consistency */
    subcontrol-position: top;
}

QScrollBar::up-arrow:vertical, QScrollBar::down-arrow:vertical {
    background: #FAFAFA; /* Changed to white */
}

QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    background: none;
}

/* CheckBox */
QCheckBox {
    color: #FAFAFA;
}

QCheckBox::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #52544C;
    background-color: #887579;
}

QCheckBox::indicator:checked {
    background-color: #BAB9BF;
}

/* RadioButton */
QRadioButton {
    color: #FAFAFA;
}

QRadioButton::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #52544C;
    border-radius: 6px; /* Makes it circular */
    background-color: #887579;
}

QRadioButton::indicator:checked {
    background-color: #BAB9BF;
}

/* TabWidget */
QTabWidget::pane {
    border: 1px solid #52544C;
    background-color: #503836;
}

QTabBar::tab {
    background: #887579;
    border: 1px solid #52544C;
    border-bottom: none; /* Removed border-bottom-color for cleaner look */
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    min-width: 8ex;
    padding: 2px;
    color: #FAFAFA; /* Changed to white for readability */
}

QTabBar::tab:selected, QTabBar::tab:hover {
    background: #BAB9BF;
}

QTabBar::tab:selected {
    border-color: #52544C;
    border-bottom-color: #BAB9BF; /* make the selected tab appear above the pane */
}

/* MenuBar */
QMenuBar {
    background-color: #887579;
    color: #FAFAFA; /* Changed to white for readability */
}

QMenuBar::item {
    spacing: 3px;
    padding: 1px 4px;
    background: transparent;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background: #BAB9BF;
}

QMenuBar::item:pressed {
    background: #52544C;
}

/* Menu */
QMenu {
    background-color: #887579;
    border: 1px solid #52544C;
    border-radius: 3px;
}

QMenu::item {
    padding: 2px 25px 2px 20px;
    border: 1px solid transparent; /* reserve space for selection border */
}

QMenu::item:selected {
    background-color: #BAB9BF;
    color: #FAFAFA; /* Changed to white for readability */
}

QMenu::separator {
    height: 1px;
    background: #52544C;
    margin-left: 10px;
    margin-right: 10px;
}