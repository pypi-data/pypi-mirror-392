[project]
name = "cadens_package"
version = "0.1.0"
description = "A benign demo package for MITRE ATT&CK coursework."
readme = "README.md"
requires-python = ">=3.7"
authors = [
    { name="Caden", email="cncpro2014@outlook.com" }
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
