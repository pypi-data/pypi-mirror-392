"""
Flask extensions for Peewee
Generated by vyte v2.0
"""
from flask_restx import Api
from peewee import DatabaseProxy
{% if auth_enabled -%}
from flask_jwt_extended import JWTManager
{% endif -%}

# Initialize extensions
api = Api(
    title='{{ name | title_case }} API',
    version='1.0',
    description='API generated by vyte with Peewee',
    doc='/'  # Swagger documentation at root
)

# Peewee database instance (will be initialized in __init__.py)
db = DatabaseProxy()

{% if auth_enabled -%}
jwt = JWTManager()
{% endif -%}
