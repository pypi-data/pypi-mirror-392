[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "meshagent"
description = "Meshagent"
requires-python = ">=3.13"
license = "Apache-2.0"
keywords = []
dependencies = [
  
]

dynamic = ["version", "readme"]

[project.urls]
Documentation = "https://docs.meshagent.com"
Website = "https://www.meshagent.com"
Source = "https://www.meshagent.com"

[tool.setuptools.packages.find]
include = ["meshagent"]

[tool.setuptools.package-data]
"meshagent" = ["py.typed", "*.pyi", "**/*.pyi", "**/*.js"]

[tool.setuptools.dynamic]
version = {attr = "meshagent.version.__version__"}
readme = { file = "README.md", content-type = "text/markdown" }


[project.optional-dependencies]
all = [
  "meshagent-api[all]~=0.6.12",
  "meshagent-agents[all]~=0.6.12",
  "meshagent-cli[all]~=0.6.12",
  "meshagent-computers~=0.6.12",
  "meshagent-livekit~=0.6.12",
  "meshagent-mcp~=0.6.12",
  "meshagent-openai~=0.6.12",
  "meshagent-otel~=0.6.12",
  "meshagent-tools~=0.6.12"
]

agents = [
  "meshagent-agents[all]~=0.6.12",
]

tools = [ 
  "meshagent-tools~=0.6.12",
]

cli = [
  "meshagent-cli[all]~=0.6.12"
]

mcp-service = [
  "meshagent-cli[mcp-service]~=0.6.12"
]

otel = [
  "meshagent-otel~=0.6.12",
]
