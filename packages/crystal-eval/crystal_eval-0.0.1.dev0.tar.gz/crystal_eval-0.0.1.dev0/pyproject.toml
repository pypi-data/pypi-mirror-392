[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crystal-eval"
version = "0.0.1.dev0"  # 改成占位版本
description = "Crystal Evaluation - under development"
authors = [{name = "Your Name", email = "your@email.com"}]  # 加上作者信息
requires-python = ">= 3.10"
dependencies = []  # 暂时清空依赖