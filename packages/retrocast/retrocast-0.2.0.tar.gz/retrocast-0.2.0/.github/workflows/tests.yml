name: Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            ~/.uv
            .venv
          key: ${{ runner.os }}-uv-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Create and activate virtual environment
        run: |
          uv venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: uv sync

      - name: run tests and generate coverage report
        id: coverage
        run: |
          report=$(pytest --cov=retrocast --cov-report=term-missing)
          coverage json -o coverage.json # create the json artifact

          # this is some bash magic to make the multiline report available to next steps
          echo "report<<EOF" >> $GITHUB_OUTPUT
          echo "$report" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: find changed files in src
        id: changed-src-files
        if: github.event_name == 'pull_request'
        uses: tj-actions/changed-files@v46 # check for the latest version, they update a lot
        with:
          files: src/** # or whatever your source directory is

      - name: commit coverage.json
        if: github.ref == 'refs/heads/master' # only update badge on master branch pushes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update coverage.json"
          file_pattern: coverage.json
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"

      - name: print coverage report to logs
        run: |
          echo "${{ steps.coverage.outputs.report }}"

      - name: add coverage report as a pr comment
        if: github.event_name == 'pull_request' && steps.changed-src-files.outputs.any_changed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const output = `### Pytest Coverage Report 🧪
            \`\`\`
            ${{ steps.coverage.outputs.report }}
            \`\`\`
            `;

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: output
            });
