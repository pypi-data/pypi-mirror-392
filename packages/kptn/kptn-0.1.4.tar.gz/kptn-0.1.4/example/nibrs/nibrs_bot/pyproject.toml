[project]
name = "nibrs_bot"
version = "0.1.0"
description = "Simple slackbot for tracking number of running tasks in ECS"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
  "boto3>=1.26.40",
  "requests>=2.32.4",
  "schedule>=1.2.2",
]
