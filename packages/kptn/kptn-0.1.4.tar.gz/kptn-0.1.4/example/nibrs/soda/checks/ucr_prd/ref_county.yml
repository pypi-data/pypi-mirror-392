checks for ucr_prd.ref_county:
  - row_count > 0 
  - schema:
      fail:
        when required column missing: ['county_id', 'fips_code']
        when wrong column type:
          county_id: smallint
          fips_code: character varying(5)