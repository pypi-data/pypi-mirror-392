checks for nibrs_victim_offense:
  - row_count > 0
  - schema:
      fail:
        when required column missing: ['victim_id', 'offense_id']
        when wrong column type:
          victim_id: bigint
          offense_id: bigint
