checks for ucr_prd.ref_agency_type:
  - row_count > 0
  - schema:
      fail:
        when required column missing: ['agency_type_id', 'name']
  - invalid_count(name) = 0:
      valid values: ['City', 'County', 'Federal', 'Other', 'Other State Agency', 'State Police', 'Tribal', 'University or College', 'Unknown']