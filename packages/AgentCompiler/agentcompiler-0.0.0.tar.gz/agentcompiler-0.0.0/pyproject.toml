[build-system]
requires = [
    "setuptools>=49.4.0", 
    "cython>=0.28",
]

[project]
name = "AgentCompiler"
version = "0.0.0"
description = "A Compiler for Agent Workflow"
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE" }
