[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "envnx-cli"
version = "0.1.0"
description = "EnvNX – a small CLI toolkit for environment checks, project cleanup, code search, and config sync."
authors = [{ name = "Your Name", email = "you@example.com" }]
readme = "README.md"
requires-python = ">=3.9"

[project.scripts]
envnx = "envnx.cli:main"
