# Q1

## **Question 1 – (Diabetes.csv), (Rampur.csv)**

### **1. (Using Diabetes.csv)**

- Read the data
- Display the columns with null values
- Calculate the 5-number summary for any numerical column and correlate with box plot *(Write Python code)*
- Perform a bivariate analysis between continuous variable and categorical variable using categorical plot
- Drop the rows with outlier Z-score value > 3 of any column and display the size

---

### **2. (Using Rampur.csv)**

Develop a **simple linear regression model** for predicting the **‘Discharge’** from the Rampur dataset using scikit-learn:

- Read and divide the dataset into **training (75%)** and **testing (25%)**
- Build the model to understand the impact of **Wind** on **Discharge** and display the **intercept** and **regression coefficients**
- Predict discharge using the test dataset
- Display the regression line
- 

---

### **1. Answer**

```python
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
from scipy import stats
df = pd.read_csv("Diabetes.csv")
df.head()
df.isnull().sum()
df['Glucose'].describe()[['min','25%','50%','75%','max']]
sns.boxplot(x=df['Glucose'])
plt.title("Box Plot of Glucose")
plt.show()
sns.catplot(x="Outcome", y="Glucose", kind="box", data=df)
plt.title("Glucose vs Outcome")
plt.show()
z = np.abs(stats.zscore(df.select_dtypes(include='number')))
df_clean = df[(z < 3).all(axis=1)]
df_clean.shape
```

---

### **2. Answer**

```python
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error
import numpy as np
import matplotlib.pyplot as plt

data = pd.read_csv("Rampur.csv")

X = data[['Wind']]
y = data['Discharge']

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.25, random_state=42)

model = LinearRegression()
model.fit(X_train, y_train)

intercept = model.intercept_
coef = model.coef_[0]

intercept, coef

y_pred = model.predict(X_test)

plt.scatter(X_test, y_test)
plt.plot(X_test, y_pred)
plt.xlabel("Wind")
plt.ylabel("Discharge")
plt.title("Regression Line: Wind vs Discharge")
plt.show()

mse = mean_squared_error(y_test, y_pred)
rmse = np.sqrt(mse)

mse, rms
```