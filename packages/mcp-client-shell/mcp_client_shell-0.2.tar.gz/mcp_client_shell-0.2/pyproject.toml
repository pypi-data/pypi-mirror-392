[project]
name = "mcp-client-shell"
version = "0.2"
description = "A local MCP client for testing FastMCP servers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["fastmcp>=2.13.1"]

authors = [{ name = "Matheus Cafalchio", email = "mcafalchio@gmail.com" }]
license = { text = "MIT" }

[project.scripts]
local-mcp-server = "local_mcp_server.client:main"
