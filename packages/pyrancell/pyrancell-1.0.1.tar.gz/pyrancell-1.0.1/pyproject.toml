[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyrancell"
version = "1.0.1"
authors = [
    {name = "Danial Erfan Dost"}
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
keywords = ["example", "package"]

[project.urls]
Homepage = "https://github.com/NOMASKERFAN"