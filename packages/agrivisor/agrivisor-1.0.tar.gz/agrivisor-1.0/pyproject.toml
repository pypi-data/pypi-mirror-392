[project]
name = "agrivisor"
version = "1.0"
description = "Agriculture advisory engine for weather-based recommendations"
requires-python = ">=3.10"
authors = [
 {name="Muzammil Sarwar", email="x24212857@student.ncirl.ie"}
]

dependencies = [
    "pydantic",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
