[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "parag-forest"
version = "0.0.1"
description = "A small Random Forest Regressor implemented in pure Python and numpy"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="Parag Mehta", email="mehtaparag45@gmail.com" }
]
license = { text = "MIT" }
dependencies = ["numpy>=1.20"]
