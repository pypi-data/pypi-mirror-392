[tool.poetry]
name = "simple-indian-date"
version = "0.1.0"
description = "A simple library to convert dates into Indian date formats"
authors = ["Vignesh"]
license = "MIT"
readme = "README.md"
packages = [
    { include = "simple_indian_date" }
]

[tool.poetry.dependencies]
python = ">=3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
