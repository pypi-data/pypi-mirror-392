[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bbeval"
version = "0.1.11"
description = "A lightweight, extensible evaluator for testing model-generated responses"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "dspy-ai>=2.4.0",
    "pyyaml>=6.0",
    "pathlib2>=2.3.0",
    "anthropic>=0.25.0",
    "openai>=1.30.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]
windows = [
    "pywin32>=306",
    "psutil>=5.9.0",
]

[project.scripts]
bbeval = "bbeval.cli:main"
open-vscode-workspace = "bbeval.open_vscode_workspace:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["bbeval*"]

[tool.pytest.ini_options]
testpaths = ["src/tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["bbeval"]
omit = ["src/tests/*", "setup.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
]

[dependency-groups]
dev = [
    "twine>=6.2.0",
]
