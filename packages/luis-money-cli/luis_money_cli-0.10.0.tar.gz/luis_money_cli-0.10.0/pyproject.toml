[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "luis-money-cli"
version = "0.10.0"
description = "A minimal money CLI with sqlite-backed accounts"
requires-python = ">=3.13"

[project.scripts]
money = "money.cli:main"
