[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kollabor"
version = "0.1.3"
description = "An advanced, highly customizable terminal-based chat application for interacting with LLMs"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [
    {name = "Kollabor Contributors", email = "contributors@example.com"}
]
keywords = [
    "llm",
    "cli",
    "chat",
    "terminal",
    "ai",
    "chatbot",
    "assistant",
    "kollabor",
    "plugin-system",
    "event-driven"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Communications :: Chat",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Topic :: Terminals",
    "Environment :: Console",
    "Operating System :: OS Independent",
]

dependencies = [
    "aiohttp>=3.10.11",
]

[project.optional-dependencies]
dev = [
    "black>=23.0.0",
    "mypy>=1.0.0",
    "flake8>=6.0.0",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]

[project.urls]
Homepage = "https://github.com/malmazan/kollabor-cli"
Repository = "https://github.com/malmazan/kollabor-cli"
Documentation = "https://github.com/malmazan/kollabor-cli/blob/main/docs/"
"Bug Tracker" = "https://github.com/malmazan/kollabor-cli/issues"

[project.scripts]
kollab = "kollabor_cli_main:cli_main"

[tool.setuptools]
py-modules = ["kollabor_cli_main"]
packages = {find = {where = ["."], include = ["core*", "plugins*"]}}
include-package-data = true

[tool.setuptools.package-data]
core = ["py.typed"]
plugins = ["**/*.json", "**/*.yaml", "**/*.yml"]

[tool.black]
line-length = 88
target-version = ["py312"]
include = '\.pyi?$'

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
