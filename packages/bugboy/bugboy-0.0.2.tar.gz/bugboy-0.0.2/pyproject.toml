[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bugboy"
version = "0.0.2"
description = "Simple logger helper"
authors = [
    { name = "Tarun (Guddu)" }
]
readme = "readme.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["bugboy"]
