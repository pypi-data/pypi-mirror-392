[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "alphascore"
version = "3.0"
description = "AlphaScore v3.0 — Advanced Data Quality Scoring Engine"
readme = "README.md"
requires-python = ">=3.8"

authors = [
  { name="Shivam Mishra", email="alphaq@duck.com" }
]

license = { text = "MIT" }

dependencies = [
  "pandas>=1.3",
  "jinja2>=3.0"
]

[project.scripts]
alphascore = "alphascore.cli:main"
