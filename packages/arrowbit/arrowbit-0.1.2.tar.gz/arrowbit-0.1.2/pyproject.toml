[project]
name = "arrowbit"
version = "0.1.2"
description = "arrowbit language"
authors = [
	{"name" = "happex", "email" = "110610727+okayhappex@users.noreply.github.com"}
]
license = "GPL-3.0"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
	"pillow>=10.4"
]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"