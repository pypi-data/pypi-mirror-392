[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "stm-toolkit"
version = "0.0.1"
description = "STM Toolkit: lightweight tools for loading and processing STM/STS data. (Placeholder)"
authors = [
  { name = "QMatSuite Developers" }
]
readme = "README.md"
requires-python = ">=3.8"
